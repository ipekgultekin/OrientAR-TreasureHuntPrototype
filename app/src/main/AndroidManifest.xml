<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrientAR"
        tools:targetApi="31">

        <!-- 1️⃣ Uygulama açılınca Compose’lu MainActivity açılacak -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2️⃣ Scoreboard ekranı -->
        <activity
            android:name=".ScoreboardActivity"
            android:label="Scoreboard"
            android:theme="@style/Theme.OrientAR" />

        <!-- 3️⃣ Kamera (AR) ekranı -->
        <activity
            android:name=".TreasureHuntGameActivity"
            android:label="Treasure Hunt Game"
            android:theme="@style/Theme.OrientAR" />
    </application>
</manifest>
